/*
 * Created by shadowsparky in 2019
 *
 */

package unit

import org.junit.Test
import ru.shadowsparky.client.ConnectionType
import ru.shadowsparky.client.mvvm.models.VideoModel
import ru.shadowsparky.client.mvvm.viewmodels.VideoViewModel
import ru.shadowsparky.client.mvvm.views.VideoView

class TestWifiVideoController {
//    private val view: VideoView = mock()
//    private val model: VideoModel = mock()
//    private val controller = spy(VideoViewModel(view, ConnectionType.wifi, model))
//
//    init {
//        given(view.canvas).willReturn(mock())
//    }
//
//    @Test fun testOnKeyPressedWithNullParam() {
//        controller.onKeyPressed(null)
//        verifyNoMoreInteractions(view, model)
//    }
}